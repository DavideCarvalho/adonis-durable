---
title: Getting Started
description: Run your first durable workflow in an AdonisJS app — install, configure, register a workflow, and start a run. Zero infrastructure with the in-process transport and in-memory store.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Steps, Step } from 'fumadocs-ui/components/steps';

This guide gets a workflow running in an AdonisJS app in a few minutes — no broker, no database, using the in-process transport and an in-memory store. Swap those for the [queue transport](/docs/durable/transports/queue) and the [Lucid store](/docs/durable/stores/lucid) when you're ready for production.

## 1. Install & configure

<Tabs items={['node ace', 'manual']}>

<Tab value="node ace">

```bash
node ace add @adonis-agora/durable
```

`add` installs the package, registers `@adonis-agora/durable/durable_provider` in `adonisrc.ts`, and publishes `config/durable.ts`.

</Tab>

<Tab value="manual">

```bash
npm i @adonis-agora/durable
node ace configure @adonis-agora/durable
```

`configure` runs the same codemods as `add` (register the provider, publish the config).

</Tab>

</Tabs>

The published `config/durable.ts` defaults to the in-process `memory` transport + the in-memory store, so the engine works with zero extra infrastructure. Drivers are selected by **name** from the `transports` / `stores` maps — each lazily imports its optional peer (`@adonisjs/queue`, `@adonisjs/lucid`) only when chosen:

```ts title="config/durable.ts"
import { defineConfig, transports } from '@adonis-agora/durable'

export default defineConfig({
  transport: 'memory',
  transports: {
    memory: transports.memory(),
    // queue: transports.queue({ connection: 'redis' }), // names a config/queue.ts adapter
    // db: transports.db(),
  },
  // store: 'lucid',
  // stores: { lucid: stores.lucid() },
  // leaseMs: 30_000,
})
```

<Callout type="info">
The provider binds a singleton [`WorkflowEngine`](/docs/durable/concepts/workflows-and-steps) built from this config. Resolve it from the container anywhere — a controller, a service, a start file.
</Callout>

## 2. Define a step (optional)

`ctx.step(refOrName, input)` is the durable step primitive — always dispatched, routed to a handler by **name**. Author the handler as a `@Step` class under `app/steps`, or with `defineStep` for a typed ref. Schemas are optional — add [zod](https://zod.dev) `input`/`output` to validate at the serve boundary:

```ts title="app/steps/charge.ts"
import { defineStep } from '@adonis-agora/durable'
import { z } from 'zod'

export const chargeCard = defineStep(
  'payments:charge-card',
  async (input: { orderId: string; amountCents: number }) => ({ chargeId: `ch_${input.orderId}` }),
  {
    input: z.object({ orderId: z.string(), amountCents: z.number().int() }),
    output: z.object({ chargeId: z.string() }),
    retries: 3,
  },
)
```

## 3. Register the workflow

Workflows are registered on the engine by **name** and **version** — the body is a deterministic async function. A good place to do this once at boot is a [preload file](https://docs.adonisjs.com/guides/concepts/adonisrc-file#preloads):

```ts title="start/durable.ts"
import app from '@adonisjs/core/services/app'
import { WorkflowEngine } from '@adonis-agora/durable'
import { chargeCard } from '#steps/charge'

const engine = await app.container.make(WorkflowEngine)

engine.register('checkout', '1', async (ctx, order: { id: string; total: number }) => {
  await ctx.localStep('reserveStock', async () => ({ reserved: true }))
  const charge = await ctx.step(chargeCard, { orderId: order.id, amountCents: order.total })
  const approval = await ctx.waitForSignal<{ approved: boolean }>(`approve:${order.id}`)
  if (!approval.approved) return { status: 'rejected', chargeId: charge.chargeId }
  await ctx.localStep('ship', async () => ({ shipped: true }))
  return { status: 'shipped', chargeId: charge.chargeId }
})
```

Register the preload in `adonisrc.ts`:

```ts title="adonisrc.ts"
preloads: [
  // ...
  () => import('#start/durable'),
],
```

## 4. Serve the step handler

A step is dispatched to a handler served under its **name**. A `@Step` class or `defineStep(...)` under `app/steps` is discovered and served automatically at boot — nothing else to wire. The low-level escape hatch is `transport.handle(name, fn)` on a transport you also pass the engine:

```ts title="start/durable.ts"
import { InMemoryTransport } from '@adonis-agora/durable'

// The default in-process transport (when none is configured) is an InMemoryTransport.
// In a single-process app you can register handlers on a transport you also pass to the engine:
const transport = new InMemoryTransport()
transport.handle('payments:charge-card', async (input: { orderId: string; amountCents: number }) => {
  return { chargeId: `ch_${input.orderId}` }
})
```

<Callout type="info">
For real cross-process handlers, configure the [queue transport](/docs/durable/transports/queue) in `config/durable.ts` and run a worker process that serves the same step names. The workflow code never changes.
</Callout>

## 5. Start a run

Inject the `WorkflowEngine` and start the workflow. `start` **enqueues** the run and returns immediately — the HTTP handler never blocks on workflow logic. A worker executes the body (by default, the same instance, asynchronously), runs the steps, then suspends on the approval signal; a webhook resumes it:

```ts title="app/controllers/checkout_controller.ts"
import type { HttpContext } from '@adonisjs/core/http'
import { WorkflowEngine } from '@adonis-agora/durable'
import { inject } from '@adonisjs/core'

@inject()
export default class CheckoutController {
  constructor(private engine: WorkflowEngine) {}

  async store({ request }: HttpContext) {
    const order = request.body() as { id: string; total: number }
    await this.engine.start('checkout', order, `checkout:${order.id}`)
    return { runId: `checkout:${order.id}` } // respond now; a worker runs the body
  }

  // later, from your approval webhook:
  async approve({ params }: HttpContext) {
    await this.engine.signal(`approve:${params.id}`, { approved: true }) // completes & ships
  }
}
```

Need the outcome inline? `await this.engine.waitForRun(runId)` resolves once the run **settles** — a terminal state (completed/failed/cancelled/dead) or `suspended`:

```ts
await this.engine.start('checkout', order, runId)
const result = await this.engine.waitForRun(runId) // resolves when the run settles
```

That's it — a durable workflow whose dispatched step runs in-process, that pauses for human approval and survives restarts. Next:

- [Durability & replay](/docs/durable/concepts/durability) — the one rule the model imposes.
- [Transports](/docs/durable/transports) — move steps to another process.
- [State stores](/docs/durable/stores) — persist to Postgres with Lucid.
- [The CLI](/docs/durable/cli) — run a worker, list runs, retry from the terminal.
- [Observability](/docs/durable/observability/dashboard) — the control plane, OTel, and Telescope.

## Migrating to 0.7.0

0.7.0 redesigns the `ctx` step surface into a single dispatched primitive. If you're upgrading:

<Callout type="warn" title="Breaking changes">

1. **`ctx.call(remoteStepDef, input, opts)` is gone** — use `ctx.step(refOrName, input, opts?)`. It is always dispatched and routed by step **name**.
2. **The old in-process `ctx.step(name, async () => {…}, opts)` is renamed to `ctx.localStep(...)`** — the in-process escape-hatch step (still supports `{ compensate }`).
3. **`ctx.random()` and `ctx.uuid()` are gone** — use `ctx.sideEffect(() => …)` to capture any non-determinism once (wrap your own generator). `ctx.now()` is unchanged.
4. **`remoteStep({ name, group, input, output })` / `RemoteStepDef` are gone.** Author a step three ways: a **string name** (`ctx.step('billing:charge', input)`), a **`@Step`** class method under `app/steps`, or **`defineStep(name, fn, config?)`**. Step `input`/`output` schemas are now **optional** (validated at the serve boundary when present).
5. **Per-call dispatch options** are `{ queue, priority, fairnessKey, transport, retries, backoff, backoffMs, backoffMaxMs, jitter, timeoutMs }`, and a dispatched saga **compensate** can be a `@Step`/`defineStep` ref (a `StepUndo` envelope), not just a local closure on `ctx.localStep`.
6. **Step routing is now by name** (`tenantGroup(sanitizeQueueToken(name), partition)`). The per-step transport `group` option is **deprecated** (accepted, ignored); use the optional `partition` for queue isolation.

</Callout>
